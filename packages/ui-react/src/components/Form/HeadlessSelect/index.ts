import Button from "./Button";
import Option from "./Option";
import Options from "./Options";
import Providers from "./Providers";

export const HeadlessSelect = Object.assign(Providers, {
  Button,
  Option,
  Options,
});
